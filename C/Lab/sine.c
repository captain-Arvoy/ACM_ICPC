#include <stdio.h>
int main(){
	int n, c = 1;
	printf("Enter the number of terms: ");
	scanf("%d",&n);
	for (int i = 1; i <= n; i++){
			for (c = 1; c <= n; c += 2){

			if (i % 2 == 0){
					s -= pow(x,i)//*factorial*/ ;
		    }
		    else {
					s += pow(x,i);
		    }

